# Copy to .env. Vite only exposes variables prefixed VITE_ to client code
# (CLAUDE.md's REACT_APP_ prefix is a Create React App convention and would
# be silently undefined here — this project uses Vite, not CRA).
VITE_API_URL=http://localhost:8000
